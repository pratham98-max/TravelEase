<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>TravelEase ✈️</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />

  <!-- Google Fonts -->
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600;700&display=swap" rel="stylesheet">

  <!-- Bootstrap -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

  <!-- Custom Styles -->
  <link href="/styles.css" rel="stylesheet" />

  <style>
    :root {
      --primary: #00c2ff;
      --secondary: #ff7a59;
      --accent: #4ce0b3;
      --dark: #0f1724;
      --light: #f8fafc;
    }

    body {
      font-family: "Poppins", sans-serif;
      background: url('https://images.unsplash.com/photo-1507525428034-b723cf961d3e?auto=format&fit=crop&w=1920&q=80') no-repeat center center fixed;
      background-size: cover;
      color: var(--dark);
      position: relative;
      overflow-x: hidden;
    }

    /* Overlay for readability */
    body::before {
      content: "";
      position: fixed;
      inset: 0;
      background: rgba(15, 23, 36, 0.45);
      backdrop-filter: blur(3px);
      z-index: -1;
    }

    /* Navbar */
    .navbar {
      background: rgba(0, 194, 255, 0.2);
      backdrop-filter: blur(12px);
      border-bottom: 1px solid rgba(255, 255, 255, 0.1);
      box-shadow: 0 4px 16px rgba(0,0,0,0.2);
    }

    .navbar-brand {
      font-weight: 700;
      font-size: 1.5rem;
      color: #fff !important;
      letter-spacing: 0.6px;
    }

    .navbar-nav .nav-link {
      color: #f8fafc !important;
      font-weight: 500;
      margin-right: 10px;
      position: relative;
      transition: color 0.3s;
    }

    .navbar-nav .nav-link::after {
      content: "";
      position: absolute;
      left: 50%;
      bottom: -6px;
      transform: translateX(-50%) scaleX(0);
      width: 60%;
      height: 3px;
      border-radius: 6px;
      background: linear-gradient(90deg, var(--secondary), var(--accent));
      transition: transform 0.3s ease;
    }

    .navbar-nav .nav-link:hover::after {
      transform: translateX(-50%) scaleX(1);
    }

    .navbar-nav .nav-link:hover {
      color: #fffdd0 !important;
    }

    /* Welcome text */
    .welcome-text {
      color: #ffe48f;
      font-weight: 500;
      margin-right: 15px;
    }

    /* Buttons */
    .btn-custom {
      border-radius: 25px;
      font-weight: 600;
      padding: 6px 16px;
    }
    .btn-login {
      background: #ffffff33;
      color: #fff;
      border: none;
    }
    .btn-login:hover {
      background: #ffffff55;
    }
    .btn-signup {
      background: #fff;
      color: var(--primary);
    }
    .btn-signup:hover {
      background: var(--light);
      color: var(--secondary);
    }

    /* Flash Messages */
    .alert {
      border-radius: 12px;
      font-weight: 500;
      box-shadow: 0 4px 20px rgba(0,0,0,0.1);
    }

    /* Main Container */
    main.container {
      background: rgba(255,255,255,0.85);
      border-radius: 16px;
      padding: 30px;
      box-shadow: 0 6px 28px rgba(0,0,0,0.1);
      backdrop-filter: blur(10px);
      margin-top: 30px;
    }

    /* Footer */
    footer {
      background: linear-gradient(90deg, #111827, #0b1220);
      color: #e5e7eb;
      text-align: center;
      padding: 18px 0;
      margin-top: 60px;
      font-size: 15px;
      box-shadow: 0 -4px 18px rgba(0,0,0,0.2);
    }

    footer span {
      color: var(--accent);
      font-weight: 600;
    }
  </style>
</head>

<body>

  <!-- Navbar -->
  <nav class="navbar navbar-expand-lg navbar-dark sticky-top">
    <div class="container-fluid px-4">
      <a class="navbar-brand" href="/">TravelEase ✈️</a>

      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
        <span class="navbar-toggler-icon"></span>
      </button>

      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
          <li class="nav-item"><a class="nav-link" href="/">Home</a></li>
          <li class="nav-item"><a class="nav-link" href="/home">Book Resorts</a></li>
          <li class="nav-item"><a class="nav-link" href="/reviews">Add Review</a></li>
          <% if (locals.user) { %>
            <li class="nav-item"><a class="nav-link" href="/dashboard">Dashboard</a></li>
          <% } %>
        </ul>

        <!-- Right Side -->
        <div class="d-flex align-items-center">
          <% if (locals.user) { %>
            <span class="welcome-text">Welcome, <%= locals.user.username %> 👋</span>
            <a href="/logout" class="btn btn-custom btn-login">Logout</a>
          <% } else { %>
            <a href="/login" class="btn btn-custom btn-login me-2">Login</a>
            <a href="/signup" class="btn btn-custom btn-signup">Sign Up</a>
          <% } %>
        </div>
      </div>
    </div>
  </nav>

  <!-- Flash Messages -->
  <div class="container mt-3">
    <% if (locals.success && locals.success.length > 0) { %>
      <div class="alert alert-success alert-dismissible fade show" role="alert">
        <%= locals.success %>
        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
      </div>
    <% } %>
    <% if (locals.error && locals.error.length > 0) { %>
      <div class="alert alert-danger alert-dismissible fade show" role="alert">
        <%= locals.error %>
        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
      </div>
    <% } %>
  </div>

  <!-- Page Content -->
  <main class="container">
    <%- body %>
  </main>

  <!-- Footer -->
  <footer>
    © <span>TravelEase</span> 2025 — Explore the World with Ease 🌏
  </footer>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
